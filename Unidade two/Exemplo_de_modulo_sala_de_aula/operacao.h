#ifndef OPERACAO_H
#define OPERACAO_H

void operacao_sub(float numero1, float numero2);
void operacao_soma(float numero1, float numero2);
void operacao_mul(float numero1, float numero2);
void operacao_div(float numero1, float numero2);
void operacao_pot(float numero1, float numero2);

#endif
/*

- `#ifndef OPERACAO_H`: Esta é uma diretiva de pré-processador que verifica se `OPERACAO_H` já foi definido. 

- `#define OPERACAO_H`: Esta é outra diretiva de pré-processador que define `OPERACAO_H`.

- As linhas seguintes são declarações de funções.

- Todo o código entre `#ifndef` e `#endif` é conhecido como um bloco de guarda de inclusão.


*/